# PSC-FINDINGS — Systemic Category Fidelity Oracle Audit

**Date:** 2026-05-20
**Phase:** PSC (dedicated systemic-category-fidelity pass)
**Branch:** migration-psc-systemic-categories
**Oracle:** `https://blog.mithis.net` (TLS cert expired → fetched with `-sk`/unverified; HTTP 200 confirmed for all live posts except those noted as HTTP 500 below)
**Worktree:** `/home/tim/github/mithro/blog.mithis.net/.worktrees/migration-psc-systemic-categories`

Scrape scripts and raw oracle HTML retained in `tmp/psc/` (gitignored).

---

## A. Live WP Total Post Count — Corpus Completeness Oracle

### Method Used

**Primary method: WP sitemap federation (authoritative)**

`https://blog.mithis.net/sitemap.xml` is a sitemap index generated by the Google Sitemap Generator plugin (v4.0.8). It references per-month post sub-sitemaps at `sitemap-pt-post-YYYY-MM.xml`. All 30 post sub-sitemaps were fetched and parsed.

**Confirmation method: Category-archive union (corroboration)**

All 23 live category archive pages were fetched (with full pagination). The union of all category-member sets gives 76 distinct WP post IDs — matching the `_posts/` corpus count.

### Results

**Live WP sitemap total: 75 posts**
**Category-archive union total: 76 distinct WP IDs**

The one-post discrepancy is explained by WP102 (Starhunter post):
- WP102 (`/archives/sci-fi/102-starhunter-fireflys-little-known-older-cousin`) returns HTTP 500 on the live site.
- WP102 is absent from the WP sitemap (the sitemap generator skips it — likely because it is the HTTP 500 post, either disabled or corrupted in WP).
- WP102 IS listed in the `sci-fi` category archive (HTTP 200; the category page renders even though the post itself 500s).
- WP102 is in `_posts/` as a P3-recovered post with `wayback_recovered: true`.

**Conclusion:** The live WP corpus has **75 sitemap-indexed posts + 1 HTTP-500/sitemap-excluded post (WP102) = 76 total posts**. Our `_posts/` corpus of 76 is **complete and consistent with the live oracle**. There are zero genuinely-missing posts.

### §11 "76/76" Open Question — Resolution

**RESOLVED: 76/76 is confirmed correct. No posts are missing from `_posts/`.**

- Our `_posts/` has 76 posts.
- All 75 sitemap-indexed posts are present in `_posts/`.
- WP102 is the 76th post: in `_posts/` (P3-recovered), in the sci-fi category archive, but HTTP 500 on the live post page and absent from the sitemap.
- Zero posts are in the WP sitemap but absent from `_posts/`.
- Zero posts are in `_posts/` but absent from the live oracle corpus (WP102 is in the category archive, confirming it is a valid WP post even if unreachable).

### Complete WP Post Inventory

All 76 posts by WP ID, permalink (sitemap URL or category-archive URL), and oracle status:

| WP_ID | Permalink | HTTP Status | Sitemap |
|-------|-----------|-------------|---------|
| WP3 | /archives/uncategorized/3-resume | 200 | yes |
| WP5 | /archives/ideas/5-swap-nice | 200 | yes |
| WP6 | /archives/ideas/6-gnome-improvements | 200 | yes |
| WP7 | /archives/ideas/7-nm-vpn | 200 | yes |
| WP8 | /archives/lca/8-lca-proposal-suggestions | 200 | yes |
| WP10 | /archives/tp/10-compiling-tpserver-cpp-under-windows | 200 | yes |
| WP12 | /archives/tp/12-compiling-tpserver-cpp-under-windows-part-2 | 200 | yes |
| WP13 | /archives/tp/13-compiling-tpserver-cpp-under-windows-part-3 | 200 | yes |
| WP15 | /archives/tp/15-tp-protocol-overview | 500 | yes |
| WP16 | /archives/tp/16-tailor-darcs2svn-tp | 200 | yes |
| WP19 | /archives/rcs/darcs/19-darcs-almost-perfect | 200 | yes |
| WP20 | /archives/ideas/20-graphical-programming | 200 | yes |
| WP21 | /archives/ideas/21-eagle-for-pcb | 500 | yes |
| WP23 | /archives/pcb/23-eagle2geda-symbol-converter | 500 | yes |
| WP26 | /archives/highlights/26-i-want-a-cool-desktop | 200 | yes |
| WP27 | /archives/tp/27-summer-of-code-woo | 200 | yes |
| WP28 | /archives/uncategorized/28-liferea-bug | 200 | yes |
| WP34 | /archives/uncategorized/34-doh-pictures-gone | 200 | yes |
| WP35 | /archives/tp/35-using-tailor-to-go-to-git | 500 | yes |
| WP38 | /archives/diary/38-tv-idiots | 200 | yes |
| WP40 | /archives/pcb/40-almost-there | 200 | yes |
| WP41 | /archives/uni/41-cfxs-all-done | 500 | yes |
| WP44 | /archives/uni/44-freeatlast | 200 | yes |
| WP45 | /archives/tp/45-schemepy | 200 | yes |
| WP46 | /archives/lca/46-freeplay | 200 | yes |
| WP48 | /archives/uncategorized/48-resume | 200 | yes |
| WP49 | /archives/uncategorized/49-soc-end | 200 | yes |
| WP50 | /archives/lca/50-freeplay-debrief | 200 | yes |
| WP51 | /archives/ideas/51-nm-autovpn | 500 | yes |
| WP54 | /archives/ideas/54-nm-openvpn-dns | 500 | yes |
| WP59 | /archives/tp/59-mentor-summit | 200 | yes |
| WP60 | /archives/tp/60-gsoc-results | 200 | yes |
| WP61 | /archives/lca/61-lca08-rego | 200 | yes |
| WP62 | /archives/lca/62-gaming-miniconf-cfp | 200 | yes |
| WP64 | /archives/ideas/64-python-swap-var | 500 | yes |
| WP65 | /archives/lca/65-itwire | 200 | yes |
| WP66 | /archives/lca/66-lca2008-over | 200 | yes |
| WP67 | /archives/google/67-google-patchwork | 200 | yes |
| WP72 | /archives/ideas/72-cfxs-free | 200 | yes |
| WP73 | /archives/tp/73-tp-release-v3 | 200 | yes |
| WP74 | /archives/tp/74-gsoc-2008 | 200 | yes |
| WP75 | /archives/lca/75-gaming-miniconf-videos | 200 | yes |
| WP76 | /archives/tp/76-gsoc2008 | 200 | yes |
| WP77 | /archives/uncategorized/77-epiphany2firefox | 500 | yes |
| WP79 | /archives/google/79-going-to-sydney | 500 | yes |
| WP80 | /archives/uncategorized/80-rocks | 500 | yes |
| WP81 | /archives/google/81-noogler-week-1 | 500 | yes |
| WP82 | /archives/games/82-techtalk-gamingforfreedom | 200 | yes |
| WP84 | /archives/ubuntu/84-my-three-weeks-on-a-mac | 500 | yes |
| WP87 | /archives/uncategorized/87-babylon-5-dvd-copy-protection | 500 | yes |
| WP88 | /archives/uncategorized/88-leslie-tshirts-response | 200 | yes |
| WP90 | /archives/python/90-firefox3-cookies-in-python | 500 | yes |
| WP91 | /archives/python/91-utf-8-in-python | 200 | yes |
| WP92 | /archives/uncategorized/92-power-scripts-in-intrepid | 500 | yes |
| WP93 | /archives/tp/93-in-the-land-of-the-sheep | 200 | yes |
| WP94 | /archives/python/94-reading-cookies-firefox | 500 | yes |
| WP95 | /archives/tp/95-xcompiling-cygwin-on-linux-for-windows | 500 | yes |
| WP96 | /archives/lca/96-lguest-and-ksplice | 200 | yes |
| WP97 | /archives/gaming-miniconf/97-programmer-art-its-deadly | 200 | yes |
| WP99 | /archives/games/99-osdc-orbital-death-better-late-then-never | 200 | yes |
| WP102 | /archives/sci-fi/102-starhunter-fireflys-little-known-older-cousin | 500 | **NO** (P3-recovered) |
| WP407 | /archives/useful-bits/407-fastcomplete | 200 | yes |
| WP1823 | /archives/useful-bits/1823-lightning-timer-website | 200 | yes |
| WP1833 | /archives/ubuntu/1833-connecting-to-a-fritzbox-under-linux-using-vpnc | 200 | yes |
| WP1980 | /archives/timvideos-us/1980-hdmi2usb-production-board-bring-up-snippets-prep-work | 200 | yes |
| WP1985 | /archives/timvideos-us/1985-hdmi2usb-production-board-bring-up-day-1-21st-july-2014 | 200 | yes |
| WP1988 | /archives/timvideos-us/1988-hdmi2usb-production-board-bring-up-day-2-22nd-july-2014 | 200 | yes |
| WP1993 | /archives/timvideos-us/1993-hdmi2usb-production-board-bring-up-day-3-23rd-july-2014 | 200 | yes |
| WP1995 | /archives/timvideos-us/1995-hdmi2usb-production-board-bring-up-day-4-24th-july-2014 | 200 | yes |
| WP2003 | /archives/timvideos-us/2003-hdmi2usb-production-board-bring-up-day-5-6-and-7th-25th-26th-and-27th-july-2014 | 200 | yes |
| WP2009 | /archives/timvideos-us/2009-hdmi2usb-production-board-bring-up-day-8-28th-july-2014 | 200 | yes |
| WP2045 | /archives/timvideos-us/2045-first-v2-hdmi2usb-production-board-constructed | 200 | yes |
| WP2167 | /archives/lca/2167-timvideos-us-2016-new-years-resolutions | 200 | yes |
| WP2169 | /archives/summer-of-code/2169-timvideos-us-and-google-summer-of-code-2016 | 200 | yes |
| WP2172 | /archives/useful-bits/2172-using-identitiesonly-without-key-files | 200 | yes |
| WP2186 | /archives/hardware/2186-nmigen-new-improved-by-whitequark | 200 | yes |

**HTTP 500 posts (20 total; direct oracle unreachable; category data sourced from category archives):**
WP15, WP21, WP23, WP35, WP41, WP51, WP54, WP64, WP77, WP79, WP80, WP81, WP84, WP87, WP90, WP92, WP94, WP95, WP102

**Note on HTTP 500 posts:** These posts render HTTP 500 on the live site but are visible in their category archive listings. The category-archive union is the authoritative fallback. For posts where the category archive shows the post in a specific category, that is oracle-confirmed (the category archive page renders HTTP 200 and lists the post).

### Oracle Methodology Notes

- **Direct post oracle (18/26 fixes):** Posts returning HTTP 200 — categories extracted from `rel="category tag"` anchor links in the post body. Corroborated with `s-category-*` body CSS classes. URL path `rcs/darcs` maps to slug `rcs-darcs`; `timvideos-us/hdmi2usb` maps to slug `hdmi2usb`.
- **Category-archive oracle (all posts):** 23 category archive pages fetched with full pagination (tp and lca each have 2 pages). Post IDs extracted from `id="post-NNN"` attributes. This is the authoritative corpus-completeness oracle.
- **Priority rule:** When direct oracle and archive oracle agree → `direct_oracle_confirmed`. When direct oracle is unreachable → `archive_union`. In no case did direct oracle and archive disagree (all 200-OK posts' direct categories were a subset of or equal to the archive union).

---

## B. Corpus Completeness — Missing / Extra Post Analysis

### Missing Posts (in live WP but NOT in `_posts/`)

**None.** Zero posts are missing from `_posts/`.

### Extra Posts (in `_posts/` but NOT in live WP sitemap)

**One post: WP102** — `2009-05-26-starhunter-fireflys-little-known-older-cousin.md`

- NOT in the WP sitemap (the sitemap generator excludes it, likely because it returns HTTP 500).
- IS in the `sci-fi` category archive (HTTP 200).
- IS a P3-recovered post (`wayback_recovered: true`).
- Its single category `sci-fi` matches the archive oracle.
- **Verdict: VALID — not truly "extra". WP102 is a legitimate WP post that is simply inaccessible (HTTP 500) and excluded from the sitemap. Keep it in `_posts/`.**

### Under-Categorized Posts (categories: is a proper subset of faithful set)

**26 posts require categories: updates.** All are under-categorizations (missing categories, no extra/wrong ones). See §D for copy-pasteable remediation.

### Over-Categorized / Wrong-Category Posts

**Zero.** No post has categories that contradict the oracle.

### §11 "76/76" Verdict

`Live Total = 75 (sitemap) + 1 (WP102 excluded from sitemap but oracle-confirmed in category archive) = 76`. Our `_posts/` count = 76. **76 == 76. No missing posts. No recovery needed.** The design criterion "76/76 posts" is met.

---

## C. Per-Category Before/After Audit

Oracle method: complete paginated category archive scrape (all 23 known slugs). `lca` and `tp` each have 2 pages; all others have 1 page.

| slug | current built count | live WP oracle count | Δ | posts needing category added (WP-IDs) | new cat page needed? |
|------|---------------------|---------------------|---|---------------------------------------|----------------------|
| diary | 1 | 2 | **-1** | [93] | no |
| games | 2 | 3 | **-1** | [50] | no |
| gaming-miniconf | 1 | 7 | **-6** | [46, 61, 62, 65, 66, 75] | no |
| google | 3 | 7 | **-4** | [74, 82, 84, 93] | no |
| hardware | 3 | 3 | 0 | — | no |
| hdmi2usb | 10 | 10 | 0 | — | no (exists: category/timvideos-us/hdmi2usb.md) |
| highlights | 1 | 2 | **-1** | [15] | no |
| ideas | 9 | 9 | 0 | — | no |
| lca | 10 | 12 | **-2** | [73, 95] | no |
| pcb | 3 | 5 | **-2** | [21, 72] | no |
| python | 4 | 6 | **-2** | [64, 2186] | no |
| rcs-darcs | 3 | 3 | 0 | — | no |
| scheme | 1 | 1 | 0 | — | no |
| sci-fi | 1 | 1 | 0 | — | no |
| summer-of-code | 1 | 8 | **-7** | [27, 49, 59, 60, 74, 76, 93] | no |
| sydney | 1 | 1 | 0 | — | no |
| tailor | 1 | 1 | 0 | — | no |
| timvideos-us | 10 | 10 | 0 | — | no |
| tp | 15 | 16 | **-1** | [49] | no |
| ubuntu | 2 | 3 | **-1** | [54] | no |
| uncategorized | 10 | 10 | 0 | — | no |
| uni | 2 | 5 | **-3** | [40, 72, 93] | no |
| useful-bits | 3 | 4 | **-1** | [1833] | no |

**Categories with Δ = 0 (already correct): 10** — hardware, hdmi2usb, ideas, rcs-darcs, scheme, sci-fi, sydney, tailor, timvideos-us, uncategorized

**Categories with shortfalls (Δ < 0): 13** — diary(−1), games(−1), gaming-miniconf(−6), google(−4), highlights(−1), lca(−2), pcb(−2), python(−2), summer-of-code(−7), tp(−1), ubuntu(−1), uni(−3), useful-bits(−1)

**Net-new category pages needed: NONE.** All 23 category slugs have pages in `category/`. The `hdmi2usb` sub-category page (`category/timvideos-us/hdmi2usb.md`) was added in P4 D-B. The `scheme`, `sydney`, `tailor` pages were added in P4 D-B. All category slugs mentioned in the oracle are covered by existing pages.

**Sidebar counts:** The sidebar (`_includes/sidebar.html`) already has all oracle-faithful counts for all categories. After PSC-EXEC restores the faithful `categories:` in the 26 affected posts, the built category page post counts will match the sidebar counts. No sidebar changes are required.

---

## D. Remediation Worklist — Exact OLD → NEW `categories:` per Post

All 26 posts need ONLY `categories:` front-matter changes (add missing slugs). No content changes, no post recovery needed. Listed in WP-ID order.

### Oracle source key
- `[D]` = direct oracle (HTTP 200 post page, `rel="category tag"` links + body class)
- `[A]` = archive union oracle (post HTTP 500; sourced from paginated category archive pages)

---

### WP15 — `_posts/2007-02-23-tp-protocol-overview.md` [A]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- highlights
- tp
```

**Missing:** `highlights`
**Evidence:** highlights category archive (`/archives/category/highlights`) lists WP15 with `class="category-highlights category-tp"` (HTTP 200). tp category page 2 also lists WP15.

---

### WP21 — `_posts/2007-03-07-eagle-for-pcb.md` [A]

**OLD:**
```yaml
categories:
- ideas
```

**NEW:**
```yaml
categories:
- ideas
- pcb
```

**Missing:** `pcb`
**Evidence:** pcb category archive lists WP21; ideas category archive also lists WP21. Post is HTTP 500.

---

### WP27 — `_posts/2007-03-21-summer-of-code-woo.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- summer-of-code
- tp
```

**Missing:** `summer-of-code`
**Evidence:** Direct oracle (HTTP 200): `rel="category tag"` links show `summer-of-code` + `tp`. Body class: `s-category-summer-of-code s-category-tp`. Category archives confirm both.

---

### WP40 — `_posts/2007-05-09-almost-there.md` [D]

**OLD:**
```yaml
categories:
- pcb
```

**NEW:**
```yaml
categories:
- pcb
- uni
```

**Missing:** `uni`
**Evidence:** Direct oracle (HTTP 200): `rel="category tag"` links show `pcb` + `uni`. Body class: `s-category-pcb s-category-uni`. Uni archive lists WP40.

---

### WP46 — `_posts/2007-08-15-freeplay.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`. gaming-miniconf archive lists WP46.

---

### WP49 — `_posts/2007-08-17-soc-end.md` [D]

**OLD:**
```yaml
categories:
- uncategorized
```

**NEW:**
```yaml
categories:
- summer-of-code
- tp
- uncategorized
```

**Missing:** `summer-of-code`, `tp`
**Evidence:** Direct oracle (HTTP 200): shows `summer-of-code` + `tp` + `uncategorized`. Both category archives confirm.

---

### WP50 — `_posts/2007-08-23-freeplay-debrief.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- games
- lca
```

**Missing:** `games`
**Evidence:** Direct oracle (HTTP 200): shows `games` + `lca`. Games archive lists WP50.

---

### WP54 — `_posts/2007-09-08-nm-openvpn-dns.md` [A]

**OLD:**
```yaml
categories:
- ideas
```

**NEW:**
```yaml
categories:
- ideas
- ubuntu
```

**Missing:** `ubuntu`
**Evidence:** ubuntu category archive lists WP54. Post is HTTP 500.

---

### WP59 — `_posts/2007-10-07-mentor-summit.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- summer-of-code
- tp
```

**Missing:** `summer-of-code`
**Evidence:** Direct oracle (HTTP 200): shows `summer-of-code` + `tp`. summer-of-code archive lists WP59.

---

### WP60 — `_posts/2007-10-22-gsoc-results.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- summer-of-code
- tp
```

**Missing:** `summer-of-code`
**Evidence:** Direct oracle (HTTP 200): shows `summer-of-code` + `tp`. summer-of-code archive lists WP60.

---

### WP61 — `_posts/2007-10-22-lca08-rego.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`.

---

### WP62 — `_posts/2007-10-22-gaming-miniconf-cfp.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`.

---

### WP64 — `_posts/2007-11-11-python-swap-var.md` [A]

**OLD:**
```yaml
categories:
- ideas
```

**NEW:**
```yaml
categories:
- ideas
- python
```

**Missing:** `python`
**Evidence:** python category archive lists WP64. Post is HTTP 500.

---

### WP65 — `_posts/2007-12-18-itwire.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`.

---

### WP66 — `_posts/2008-02-04-lca2008-over.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`.

---

### WP72 — `_posts/2008-02-18-cfxs-free.md` [D]

**OLD:**
```yaml
categories:
- ideas
```

**NEW:**
```yaml
categories:
- ideas
- pcb
- uni
```

**Missing:** `pcb`, `uni`
**Evidence:** Direct oracle (HTTP 200): shows `ideas` + `pcb` + `uni`. Both pcb and uni archives confirm.

---

### WP73 — `_posts/2008-02-19-tp-release-v3.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- lca
- tp
```

**Missing:** `lca`
**Evidence:** Direct oracle (HTTP 200): shows `lca` + `tp`. lca archive page 2 lists WP73.

---

### WP74 — `_posts/2008-02-26-gsoc-2008.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- google
- summer-of-code
- tp
```

**Missing:** `google`, `summer-of-code`
**Evidence:** Direct oracle (HTTP 200): shows `google` + `summer-of-code` + `tp`. Both category archives confirm.

---

### WP75 — `_posts/2008-03-06-gaming-miniconf-videos.md` [D]

**OLD:**
```yaml
categories:
- lca
```

**NEW:**
```yaml
categories:
- gaming-miniconf
- lca
```

**Missing:** `gaming-miniconf`
**Evidence:** Direct oracle (HTTP 200): shows `gaming-miniconf` + `lca`.

---

### WP76 — `_posts/2008-03-18-gsoc2008.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- summer-of-code
- tp
```

**Missing:** `summer-of-code`
**Evidence:** Direct oracle (HTTP 200): shows `summer-of-code` + `tp`. summer-of-code archive lists WP76.

---

### WP82 — `_posts/2008-06-10-techtalk-gamingforfreedom.md` [D]

**OLD:**
```yaml
categories:
- games
```

**NEW:**
```yaml
categories:
- games
- google
```

**Missing:** `google`
**Evidence:** Direct oracle (HTTP 200): shows `games` + `google`. Google archive lists WP82.

---

### WP84 — `_posts/2008-07-08-my-three-weeks-on-a-mac.md` [A]

**OLD:**
```yaml
categories:
- ubuntu
```

**NEW:**
```yaml
categories:
- google
- ubuntu
```

**Missing:** `google`
**Evidence:** google category archive lists WP84; ubuntu archive also lists WP84. Post is HTTP 500.

---

### WP93 — `_posts/2008-11-15-in-the-land-of-the-sheep.md` [D]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- diary
- google
- summer-of-code
- tp
- uni
```

**Missing:** `diary`, `google`, `summer-of-code`, `uni`
**Evidence:** Direct oracle (HTTP 200): shows all 5 categories. All 5 category archives confirm WP93. This is the post with the most categories (5 total).

---

### WP95 — `_posts/2009-01-27-xcompiling-cygwin-on-linux-for-windows.md` [A]

**OLD:**
```yaml
categories:
- tp
```

**NEW:**
```yaml
categories:
- lca
- tp
```

**Missing:** `lca`
**Evidence:** lca category archive page 2 lists WP95. Post is HTTP 500.

---

### WP1833 — `_posts/2013-10-06-connecting-to-a-fritzbox-under-linux-using-vpnc.md` [D]

**OLD:**
```yaml
categories:
- ubuntu
```

**NEW:**
```yaml
categories:
- ubuntu
- useful-bits
```

**Missing:** `useful-bits`
**Evidence:** Direct oracle (HTTP 200): shows `ubuntu` + `useful-bits`. useful-bits archive lists WP1833.

---

### WP2186 — `_posts/2020-05-02-nmigen-new-improved-by-whitequark.md` [D]

**OLD:**
```yaml
categories:
- hardware
```

**NEW:**
```yaml
categories:
- hardware
- python
```

**Missing:** `python`
**Evidence:** Direct oracle (HTTP 200): shows `hardware` + `python`. python archive lists WP2186.

---

### D.1 Posts Grouped by Category Being Restored

For implementer convenience, the same 26 posts organized by which category is being added:

**`gaming-miniconf` (6 posts):** WP46, WP61, WP62, WP65, WP66, WP75

**`summer-of-code` (7 posts):** WP27, WP49, WP59, WP60, WP74, WP76, WP93

**`google` (4 posts):** WP74, WP82, WP84, WP93

**`uni` (3 posts):** WP40, WP72, WP93

**`lca` (2 posts):** WP73, WP95 — NOTE: WP95 is from lca archive page 2 (archive_union method)

**`pcb` (2 posts):** WP21, WP72

**`python` (2 posts):** WP64, WP2186

**`tp` (1 post):** WP49

**`diary` (1 post):** WP93

**`games` (1 post):** WP50

**`highlights` (1 post):** WP15 — NOTE: archive_union method (post HTTP 500)

**`ubuntu` (1 post):** WP54 — NOTE: archive_union method (post HTTP 500)

**`useful-bits` (1 post):** WP1833

**`hardware` (0 new posts):** already correct

### D.2 Genuinely Missing Posts — P3-style Recovery List

**None.** Zero posts are missing from `_posts/`. No P3-style recovery is required.

### D.3 Strictly Sequenced Operations

**No strict sequencing required.** All PSC-EXEC changes are front-matter-only `categories:` additions to existing posts. They are independent of each other. No content changes. No new files. The content linter (`uv run python -m scripts.fidelity.run`) will remain at 0 findings because front-matter edits do not affect content linter checks.

---

## E. Acceptance Gates (PSC Exit)

For PSC to be considered complete, ALL of the following must hold:

1. **Category counts match oracle:** For every category slug, the built post count == the live WP oracle count (from §C above). Verify with `uv run python -m scripts.fidelity.run` structure_check or by inspecting `_site/archives/category/<slug>/index.html`.

2. **Per-post categories faithful:** For every post in §D, `categories:` == oracle-confirmed faithful set. Verify via the analysis script (`tmp/psc/psc_definitive_analysis.py`).

3. **Content linter: 0 findings:** `uv run python -m scripts.fidelity.run` (or `uv run python tmp/p4_lint.py`) reports 0 findings. Front-matter-only edits must not introduce any new LIQUID_LEAK, MISSING_IMAGE, or BLOCK_HTML findings.

4. **`structure_check` 6/6:** All 6 structural archetypes pass.

5. **`bundle3.3 exec jekyll build` clean:** Zero errors, zero warnings.

6. **pytest 45 green:** `uv run python -m pytest tests/ -q` passes all 45 tests.

7. **76 posts in `_posts/`:** Count unchanged (no posts added or removed, only front matter edited).

8. **Sidebar counts match:** After the build, the sidebar's hardcoded `(N)` counts in `_includes/sidebar.html` should match the built category page post counts. The sidebar currently already has oracle-faithful counts (e.g., `gaming-miniconf (7)`, `summer-of-code (8)`, `google (7)` etc.). After PSC-EXEC, the built counts will equal the sidebar counts. **No sidebar changes are needed** — the sidebar was already updated to oracle-faithful values in P4 D-B.

   The complete oracle-faithful sidebar count table (for verification):

   | Category | Oracle count (= sidebar value = built target) |
   |----------|------|
   | Diary | 2 |
   | Games | 3 |
   | Gaming Miniconf | 7 |
   | Google | 7 |
   | Hardware | 3 |
   | Highlights | 2 |
   | Ideas | 9 |
   | linux.conf.au | 12 |
   | PCB | 5 |
   | Python | 6 |
   | RCS (rcs-darcs/darcs) | 3 |
   | Sci Fi | 1 |
   | Scheme | 1 |
   | Summer of Code | 8 |
   | Sydney | 1 |
   | Tailor | 1 |
   | TimVideos.us | 10 |
   | HDMI2USB | 10 |
   | Ubuntu | 3 |
   | Uncategorized | 10 |
   | Uni | 5 |
   | Useful Bits | 4 |

   **NOTE on sidebar `lca` count:** The sidebar currently shows `linux.conf.au (10)` but the oracle count is 12. After PSC-EXEC adds `lca` to WP73 and WP95, the built lca count will be 12. **The sidebar `lca` entry must be updated from `(10)` to `(12)`** in `_includes/sidebar.html`. This is the only sidebar count change required by PSC.

   Similarly: verify `tp` sidebar count. Oracle = 16 posts. After adding `tp` to WP49, built count = 16. Sidebar currently shows `tp` but it's not in the sidebar dropdown. Check — `tp` (Thousand Parsec) is not in the sidebar dropdown but that may be intentional.

---

## F. Artifacts

All scrape scripts and raw oracle HTML are in `tmp/psc/` (gitignored):

| File | Description |
|------|-------------|
| `tmp/psc/psc_inventory.py` | Extract `_posts/` inventory (wp_id, categories, permalink) |
| `tmp/psc/psc_fetch_sitemap.py` | Fetch main sitemap index |
| `tmp/psc/psc_parse_sitemaps2.py` | Parse all 30 post sub-sitemaps for post URLs |
| `tmp/psc/psc_fetch_categories.py` | Fetch category archive pages (initial, page 1 only) |
| `tmp/psc/psc_fetch_all_cat_pages.py` | Fetch ALL category archive pages with pagination |
| `tmp/psc/psc_verify_categories.py` | Fetch individual post pages for direct category oracle |
| `tmp/psc/psc_fetch_remaining_posts.py` | Fetch all post pages for category data |
| `tmp/psc/psc_check_rcs_cats.py` | Investigate WP16/WP19 RCS hierarchical URL structure |
| `tmp/psc/psc_check_tp_pagination.py` | Verify TP/LCA category pagination |
| `tmp/psc/psc_check_wp15.py` | Investigate WP15 highlights/tp classification |
| `tmp/psc/psc_check_counts.py` | Compare current counts vs oracle per slug |
| `tmp/psc/psc_definitive_analysis.py` | **Main analysis: faithful categories for all 76 posts** |
| `tmp/psc/oracle_sitemap.xml` | WP sitemap index |
| `tmp/psc/oracle_sitemap-pt-post-*.xml` | Per-month post sub-sitemaps (30 files) |
| `tmp/psc/oracle_cat_<slug>_p<N>.html` | Category archive HTML pages (23 slugs × 1-2 pages) |
| `tmp/psc/oracle_post_<id>.html` | Individual post HTML pages |
| `tmp/psc/oracle_categories_complete.json` | Complete paginated category membership data |
| `tmp/psc/oracle_all_post_cats.json` | Per-post oracle categories from direct fetches |
| `tmp/psc/psc_definitive.json` | Full analysis output (needs_update, correct, faithful) |

---

## G. User-Decision Items

### No blocking user-decision items.

All 26 under-categorized posts have oracle-confirmed faithful category sets. No post has ambiguous or conflicting oracle data.

**Notes on previously flagged user-decisions from P4-FINDINGS:**

1. **Sydney post (WP79) categories — RESOLVED:** WP79 (`going-to-sydney`) is already correctly set to `categories: [google, sydney]` in `_posts/`. The google and sydney category archives both confirm this is correct. No change needed. **NOT in the 26 posts requiring updates.**

2. **Tailor post (WP35) categories — RESOLVED:** WP35 (`using-tailor-to-go-to-git`) is already correctly set to `categories: [rcs-darcs, tailor, tp]` in `_posts/`. The rcs-darcs, tailor, and tp category archives all confirm this. **NOT in the 26 posts requiring updates.**

3. **tailor-darcs2svn (WP16) rcs-darcs category — RESOLVED:** WP16 already has `categories: [rcs-darcs, tp]` in `_posts/`. The direct oracle (HTTP 200) confirms `rcs/darcs` + `tp`. **NOT in the 26 posts requiring updates.**

---

## H. Concerns and Notes

### Note on WP49 `uncategorized` category

WP49 (`soc-end`) currently has `categories: [uncategorized]`. The faithful set is `[summer-of-code, tp, uncategorized]`. The direct oracle confirms all three categories via `rel="category tag"` links and body class `s-category-summer-of-code s-category-tp s-category-uncategorized`. Adding `summer-of-code` and `tp` to a post that is also `uncategorized` is technically correct per WP — WP allows posts to be in multiple categories including `uncategorized`. This is an unusual combination but is what the live oracle shows.

### Note on WP73 in `lca` archive

WP73 (`tp-release-v3`, "Thousand Parsec primary client 0.3.0 released!") is in the lca category. This may seem surprising for a TP release post, but the direct oracle (HTTP 200) confirms `lca` + `tp` categories via `rel="category tag"` links. The post dates from 2008-02-19, right after LCA 2008 (Jan 2008), and the TP client release was likely announced at or around the conference. The oracle is authoritative.

### Note on lca sidebar count

The sidebar currently shows `linux.conf.au (10)`. After PSC-EXEC adds `lca` to WP73 and WP95, the correct oracle count is 12. **The sidebar must be updated to `linux.conf.au (12)`**. This is flagged in §E acceptance gate 8.

### Note on archive_union reliability

For the 8 posts where the category data comes from archive_union only (WP15, WP21, WP54, WP64, WP84, WP95 are the ones needing updates; WP23, WP41, WP51, WP77, WP80, WP81, WP87, WP90, WP92, WP94 are already correct), the category archive pages are HTTP 200 and the post IDs are explicit `id="post-NNN"` attributes — not inferred. This is high-confidence data. The only lower-confidence case would be if a post belonged to a category but the category archive itself is unreachable — but all 23 category archives returned HTTP 200.

---

## Summary

| Metric | Value |
|--------|-------|
| Live WP total posts (oracle method) | **75** (sitemap) + 1 (WP102 excluded but oracle-confirmed) = **76** |
| Posts in `_posts/` | **76** |
| Missing posts (in live, not in `_posts/`) | **0** |
| Extra posts (in `_posts/`, not in live) | **0 valid** (WP102 is legitimate) |
| Posts needing `categories:` updates | **26** |
| Categories affected (with shortfalls) | **13** |
| New category pages needed | **0** (all 23 slugs have existing pages) |
| Sidebar count changes needed | **1** (lca: 10 → 12) |
| User-decision items | **0** |
| Largest per-category deficit | gaming-miniconf (−6), summer-of-code (−7) |
| Most-categorized post needing fix | WP93 (missing 4 categories: diary, google, summer-of-code, uni) |
